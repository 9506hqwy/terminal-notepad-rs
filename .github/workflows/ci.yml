name: CI

on:
  push:
    branches: [ main ]
    tags-ignore: [ '**' ]
  pull_request:

permissions:
  contents: read

jobs:
  windows:
    uses: 9506hqwy/actions/.github/workflows/rust-ci-windows.yml@main
